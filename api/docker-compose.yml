services:
  postgres:
    image: postgres:17
    container_name: db_postgres
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: os
    ports:
      - '5432:5432'
